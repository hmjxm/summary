unix是linux的鼻祖，unix先开始用B语言，但跨平台性太差，最后研究出了C语言，unix7代以前都是开源，7代开始闭源，即不开放源代码，之后出现minix，即小型的unix，用于教学，不商业化，之后就出现了linux，不断提出意见加以改进

unix经常用于大型的主机或服务器
linux用于个人px

unix版本：freebds经常
linux：ubuntu、redhat、fedora、suse、centos

发行版指的是使用的是linux内核，但加入了一些不同的应用程序

操作系统是用来控制硬件，并且可以让软件在上面运行

跨平台性：不依赖于操作系统，也不依赖硬件环境。即在windows、linux、mac系统上都可以运行，在不同硬件的pc上都能运行，联想可以，惠普也可以等等
开源：源代码是完全开放的
闭源：不开放源代码，能看到只是编译之后的一些代码


sudo +命令 可以使用root权限
sudo password root 给root用户设置密码
sudo su 直接进入到root用户
sudo -s 直接进入root用户
su 进入root用户，需要密码
sudo apt-get remove 。。卸载软件
sudo apt-get install。。 安装软件


ubuntu初始没有ssh服务，需要下载
sudo apt-get install openssh-server
查看进程
ps -e|grep ssh
然后重启服务
/etc/init.d/ssh start
此时，winscp和xshell均可连接ubuntu

ubuntu初始不允许Xshell使用root用户连接
需要在配置文件中修改：
/etc/ssh/sshd_config中将PermitRootLogin without-password将without-password改为yes
然后重启服务即可连接


ubuntu中ctrl+alt+f7从命令框转到桌面
       ctrl+alt+f2-6都可从桌面转到命令框
使用exit退出当前用户
ctrl+alt+T可以直接弹出终端

centos是ctrl+alt+f1和f2进行转换
使用logout退出当前用户


ubuntu中默认不允许root用户登录
需要做以下操作才可以在登录界面输入root用户名：
输入sudo gedit /usr/share/lightdm/lightdm.conf.d/50-ubuntu.conf
在弹出的编辑框gedit里输入：greeter-show-manual-login=true 保存关闭。
然后设置root用户密码
重启即可


ubuntu中若以root用户登录，则可以使用su hmj切换到hmj用户



/home是放普通用户的，里面可以在桌面上创建东西
/root是放root用户的相关文件
不同用户桌面是不一样的，根据自己添加的内容来区分

创建桌面快捷方式：
/usr是用来放一些应用程序的，在/usr/share/applications中就有很多每个用户都可以共享的应用程序，选择某个应用程序，复制，然后粘贴到桌面即可。

/bin下放的是一些可执行文件，即常用普通命令，就是我们在终端敲入的命令，如：ls、pwd等等
/sbin要具有一定权限才可以使用的命令

/mnt默认挂载光驱和软驱的目录
/boot存放系统引导的相关文件
/etc存放配置相关的文件
/var存放经常变化的文件
/usr存放程序默认安装的文件夹
/dev存放设备信息，即硬盘一些信息

指定运行级别：0-6
init 级别
0：关机
1：单用户
2：多用户状态没有网络服务
3：多用户状态有网络服务
4：系统未使用保留给用户
5：图形界面
6：系统重启

更改级别：/etc/inittab的id：5：initdefault这一行的数字


ctrl+shift+‘+’可以让终端里的字变大
ctrl+‘-’变小
ubuntu的菜单栏是固定在dash面板上方，windows是每打开一个程序，程序里面会有菜单栏
ls+文件夹名：可以直接看到文件夹里面的东西，而不需要再先进文件夹再看
clear清屏
cd -相当于跳回到上一次的路径，跟遥控器的返回键类似
cd 波浪线表示跳回当前用户的家目录下，如/home/hmj

ls -a显示隐藏文件，文件名以.开头就是隐藏文件
ls -l以列表的方式显示文件，不显示隐藏文件
ls -l -h把列表显示中的文件大小以一种更加可读的方式显示出来，6k等
ls -l、a、h无先后顺序，ls-lh,ls-alh均可
ls *.txt查找txt文件
tree是以目录树的形式显示当前目录下的文件
*表示任意多个字符，有或没有都可以；？表示一个字符

重定向：重新确定输出的方向，本来显示在终端，现在放到文件中，使用>
ls > test.txt,把当前ls输出放到test.txt中，会覆盖源文件
ls >> test.txt会把ls输出追加到tets.txt中


more test.txt能分屏显示文件，按空格显示下一屏
管道命令：把一个命令的输出作为另一个命令的输入
ls -alh | more


mkdir 创建文件夹
mkdir A/B/C -p 可直接创建A，再在A中创建B，再在B中创建C文件夹

rmdir 删除文件夹（删除空的文件夹）

touch 创建文件

rm删除所有的东西，并且是永久删除（文件和文件夹都可以删）
rm后如果跟文件，则可直接删
rm 文件名 -i代表删除之前给出提示
rm 文件名 -f代表强制删除，即使没有此文件也强制删除，不用给出提示

rm后如果跟文件夹，则需要加个选项(-r)，即rm test -r（test为文件夹名，非空文件夹也可以），-r代表递归的删除，如果文件夹里还有文件夹，就逐层的删除，最后使得最外面的文件夹及里面的文件全部删除

ln创建链接文件，即相当于创建快捷方式

软链接：不占用磁盘空间，源文件删除则失效
ln -s 源文件 链接文件
源文件修改，链接文件也会修改
链接文件修改，源文件也会修改
源文件删除，则链接文件失效
ls显示链接文件为‘l’，即链接文件

硬链接：只能链接普通文件，不能链接目录
ln 源文件 链接文件
ls显示链接文件为普通文件
源文件修改，链接文件也修改
链接文件修改，源文件也会修改
但是源文件删除，链接文件不会失效，还是可以查看

一个文件是由文件名和文件内容构成
软链接指的是创建一个链接文件也指向源文件的内容，所以源文件删除的时候，即链接文件指向的文件内容被删除，所以链接文件被删除
硬链接指的是为源文件指向的文件内容又取了一个名字，源文件和链接文件两个文件名共用一份文件内容，所以当源文件删除时，由于硬链接数为2，此时只会删除源文件名，而文件内容并没有删除，所以链接文件还是有效。

ls -l的第二列指得就是硬链接数，即此个文件内容有几个文件名共用，如果硬链接数为1，则删除文件名时其实也把其所指向的文件内容也删除了

cat查看文件内容（不分屏显示）
more查看文件内容（分屏显示）

cat可以使用重定向方式实现复制：
cat 文件名>文件名

cat也合并多个文件为一体：
cat 1.py 2.py > a.txt
则把1和2两个文件的内容全部放到了a.txt中

grep在文件中查找某些内容：
grep -n 'a' test.txt
-n表示显示匹配行及行号
-i表示忽略大小写
-v表示取反，除了含有a的其他内容
也支持正则表达式

命令+（--help）可以查看此命令的帮助文档
man+命令也可以查看
man中包含8个部分，linux也是用c语言写的，所以可以查看很多c语言函数，一般函数都在第2、3部分，可以直接通过章节号来查询，如：
man 3 printf 可以查到printf函数的信息
linux命令一般在第一章节，所以也可使用man 1 +命令来查询命令相关信息

history为查询历史命令

find指的是在指定路径下找文件
find  路径 -name 文件名
           -size 文件大小（+2M指大于2M，-2M指小于2M，2M指等于2M）
           -perm ***（查找具有某种权限的文件）    
如果不记得路径，则使用根目录/


cp拷贝文件
复制和硬链接区别：
复制有两份数据内容，一份改变不影响另一份
硬链接只有一份数据，一份修改了，另一个文件也会发生变化
cp 源文件 目标路径 -（a/f/i/v/r）
a表示保持文件原有属性，都可复制过去
f表示已经存在的目标文件而不提示
i表示交互式的复制
v表示显示复制进度
r表示可以直接把文件夹放入目标目录，如：cp b a/ -r 把b文件夹和里面的所有内容都复制到a文件夹下   cp b/* a/ -r 表示只把b文件夹下的内容复制到a中

mv剪切、移动文件
mv也可以实现重命名功能，mv 1.txt 2.txt即把1.txt重命名为2.txt
mv -(i/f/v)
i表示会给出提示
f表示不给出提示
v显示移动进度


tar归档管理：（只能打包，不能压缩）
打包：tar -cvf  包名.tar +需要打包的文件(*即所有文件都要打包)
解包：tar -xvf 包名.tar

gzip压缩解压：
压缩：gzip 包名.tar（使包的大小变小）压缩后包名变为了包名.tar.gz
解压缩：gzip -d 包名.tar.gz

打包-》压缩-》解压缩-》解包

tar -zcvf test.tar.gz * 把当前所有文件打包压缩到test.tar.gz中（既打包又压缩）
tar -zxvf test.tar.gz -C +解压路径 解压缩解包到指定路径（大写C）

如果包名为.tar.bz2结尾，则把打包压缩和解压解包命令中的-zcvf/-zxvf改成-jcvf/-jxvf即可

如果是rar，需要在ubuntu中下载专门解rar的工具

压缩为.zip：
zip myzip *
解压：
unzip -d ./(解压后的路径) myzip.zip

which +命令：查看命令所在的位置，一般都是在/bin/或/usr/bin中

linux是多用户多任务的系统
多用户：多个用户同时能登录系统
多任务：即多进程，多个应用程序可以同时运行

windows的pc版本是假的多用户多任务的系统，它只是多任务的
但windows的server版本是多用户多任务的系统

虚拟机的“编辑”选项可以查看虚拟网络编辑器，可以看到有三种模式：
桥接模式：通过主机网卡，架设了一条桥，直接连入到网络中了。因此，它使得虚拟机能被分配到一个网络中独立的IP，所有网络功能完全和在网络中的真实机器一样。
虚拟机与主机：可以相互访问，因为虚拟机在真实网络段中有独立IP，主机与虚拟机处于同一网络段中，彼此可以通过各自IP相互访问。
虚拟机与其他主机：可以相互访问，同样因为虚拟机在真实网络段中有独立IP，虚拟机与所有网络其他主机处于同一网络段中，彼此可以通过各自IP相互访问。
虚拟机之间：可以相互访问。

NAT（网络地址转换模式）：Vhost访问网络的所有数据都是由主机提供的，vhost并不真实存在于网络中，主机与网络中的任何机器都不能查看和访问到Vhost的存在。
虚拟机与主机：虚拟机可以通过网络访问到主机，主机无法通过网络访问到虚拟机。
虚拟机与其他主机：虚拟机可以访问到网络中其他主机，其他主机不能通过网络访问到虚拟机。
虚拟机之间：相互不能访问，虚拟机与虚拟机各自完全独立，相互间无法通过网络访问彼此。

仅主机模式：可以理解为Vbox在主机中模拟出一张专供虚拟机使用的网卡，所有虚拟机都是连接到该网卡上的，我们可以通过设置这张网卡来实现上网及其他很多功能，比如网卡共享、网卡桥接等。
虚拟机与主机：默认不能相互访问，双方不属于同一IP段，host-only网卡默认IP段为192.168.56.X 子网掩码为255.255.255.0，后面的虚拟机被分配到的也都是这个网段。通过网卡共享、网卡桥接等，可以实现虚拟机于主机相互访问。
虚拟机其他主机：默认不能相互访问，通过设置，可以实现相互访问。
虚拟机之间：默认可以相互访问，都是同处于一个网段。

ifcongfig可以看到虚拟网卡的一些信息

ssh ubuntu的用户名@ubuntu的ip地址

who命令可以看到登录当前系统的用户信息
who -q命令会显示用户数
whoami命令查看当前的用户
exit退出当前用户，相当于注销


useradd添加用户
useradd 用户名 -m(自动创建家目录) -d(指定家目录) /home/一般和用户名相同，这个时候会自动创建这个用户所在的用户组，且用户组名和用户名相同
useradd 用户名 -m(自动创建家目录) -d(指定家目录) /home/一般和用户名相同 -g 用户组名，此时会创建指定的用户组

ctrl+a 光标跳到命令行头
ctrl+e 光标跳到命令尾

为用户设置密码passwd 用户名
su 用户名为切换账户，但是跳转过去的账户中输入pwd还是之前用户的那个当前路径
su - 用户名切换账户跳转过去的账户中输入pwd得到的是跳转账户的当前路径，即家目录
cd 波浪线 跳转到当前用户的家目录，一般为/home/用户名

userdel删除用户
userdel 用户名即删除用户，不让这个用户登录，但不会删除用户的家目录，即/home/用户名这个文件夹不会删除
userdel -r 用户名即删除用户，同时删除家目录

sudo是添加权限，su切换账户
redhat、centos可以直接通过su root进入root用户

sudo第一次使用需要密码，在一定时间内不需要用密码，过一段时间又需要用密码

sudo -s直接转到root权限，进入root用户

$结尾的是普通用户，#结尾是超级用户

cat /etc/group
打印出来用户组组名：加密：用户组ID(唯一的)：用户名
groupmod 加两次TAB键也可以查看用户组组名

cat /etc/passwd打印出来所有用户信息
用户名：用户密码：用户ID：用户所在组ID：用户主目录：用户所使用的shell

ls -l打印出：
第一列是权限，第三列是哪个用户创建的这个文件且拥有这些权限，第四列是用户组，即用户组里的用户也拥有这些权限

groupadd 用户组名即创建用户组
groupdel 用户组名即删除用户组

修改用户所在组：usermod
usermod -g 用户组 用户名

查看用户所在哪些组：groups 用户名

usermod -a -G 用户组 用户名即将此用户添加到其他组，一个账户可以有多个用户组
但是-g那个是默认的主要组
ls打印的第四列用户组不会显示所有组，而显示默认的主要组


useradd创建的用户默认是不能使用sudo的，即没有root用户权限，不能查看root目录
提供一些安全性，普通开发者适合

sudo usermod -a -G adm 用户名
sudo usermod -a -G sudo 用户名
这两步即可以将普通用户添加上sudo权限

一个空文件夹的硬链接数是2，因为里面有两个文件夹，分别是.和..

文件权限：文件拥有者、文件所在组的其他用户、文件其他组的权限

修改文件权限：chmod
u 所有者；g 同组者；o 其他组；a 这三个全部的权限
+ 增加权限；- 撤销权限；=设定权限
chmod u+rwx,g+rwx,o+rwx 文件名 
chmod u=rwx,g=rwx,o=rwx 文件名
使用数字也可以：r 读 4;w 写 2;x 运行 1

chmod 权限 文件夹名只会修改文件夹本身的权限，而不会修改文件夹里的文件的额权限
chmod 权限 文件夹 -R 可以修改文件夹及里面所有文件的权限

chown修改文件所有者

chgrp修改文件所属用户组

系统相关命令：
cal 查看日历
date 显示具体时间
重新修改当前时间：date MMDDhhmmYY.ss(秒) +format
显示时间格式：date '+%Y-%m-%d-%H-%M-%S'

ps查看进程
ps -aux显示所有的进程

top动态的显示运行中的进程，在指定的时间间隔更新显示信息
top -d来指定更新信息的时间间隔
q退出

kill 进程ID为结束进程
先通过ps查询进程的PID，通过最后一列的command来查得
kill -9 进程ID为强制杀死

reboot为重启
shutdown -h now 立即关机
shutdown -h 20：00 某个时间关机
shutdown -h +10 多少分钟后关机
init 0 关机
init 6 重启


du查看当前目录大小
du -h

ifconfig查看网卡信息
ifconfig 网卡名 down 把网卡关闭
ifconfig 网卡名 up 把网卡开启
ifconfig 网卡名 ip地址 修改网卡的IP地址

gedit编辑器
sublim编辑器

先有vi编辑器，后有vim
vi有三种模式：
1、默认模式是命令模式
2、编辑模式即插入模式 insert键命令-》编辑，esc键编辑-》命令
3、末行模式，在命令模式下输入:命令可以保存等，wq保存退出(也可以用x)
在命令模式下，按shift+按两次z也可以直接保存退出

ctrl+n为补全功能

命令模式下：
M光标移到中间行
L光标移到最后一行行首
yy复制
8yy从当前光标所在的行开始复制8行
p粘贴
dd剪切，2dd从当前光标所在的行开始剪切8行，此时，按下u就可以撤销
G跳到最后一行
2G跳到第2行
gg跳转到第一行

{或}按段往前或往后跳转
w向后移动一个词
b向前移动一个词

命令模式下按shift+两次小于号向左缩进4个空格
命令模式下按shift+两次大于号向又缩进4个空格

.重复之前最近的那个命令
整体向右缩进：先按v进入视图模式，在选择需要移动的部分，按shift+>或<来移动，然后可以直接按.来重复移动操作

命令模式下，按r可以替换当前光标所在的字符

命令模式下输入/，在写需要查找的东西，按enter键即可查找到
按n下一个，按N上一个

替换命令：
:%s/abc/123/g 将所有abc替换成123
:1,10/abc/123/g 将1到10行的abc替换成123

:!后面跟命令，则命令可以直接执行

/etc/apt/source.list中显示的是我们下载的东西从此文件中的链接下载的
可以在网上下载镜像替换此文件的内容，使下载从国内的地址下载，会快一些

apt-get update 更新下载源，即将覆盖的文件生效

安装软件：
apt-get install 软件名称

删除软件
apt-get remove 软件名称


linux常用服务器：
ftp服务器
sudo apt-get install vsftpd
ssh服务器，远程登录，sshd服务，监听端口号为22号
scp服务器

samba服务器(局域网使用比较好)
sudo apt-get install samba samba-common
安装成功后在home路径下创建share文件夹
修改share文件夹的权限为777
修改/etc/samba/smb.conf配置文件：
[share]
    path = /home/share
    available = yes
    browseable = yes
    writeable = yes
再创建samba账户：
touch /etc/samba/smbpasswd
smbpasswd -a 账户名称
再设置samba账户密码
重启服务：./etc/init.d/smbd restart
服务器端就可以用了

在客户端运行窗口输入\\ubuntu的ip
再输入创建的samba账户名称和密码，即可在宿主机上看到share文件夹

使用samba可以免去上传下载的时间，在宿主机上操作share文件夹，只要保存，ubuntu中也会相应修改，在ubuntu中修改也是同样如此


shell：linux内核的外壳，将命令解释成linux内核可以执行的语句
相当于windows下的.bat批处理程序，在这里面写一些命令，可以直接执行
.bat文件中写一些windows命令，就相当于在cmd窗口中输入一些命令运行
C:\Users\用户名\AppData\Roaming\Microsoft\Windows\Start Menu查看开始菜单，
开始菜单中有启动，即开机时自启动，因此，先把.bat程序创建一下快捷方式，然后可以把此快捷方式丢到启动文件夹中，则系统启动就会运行.bat程序

ls -l /bin/*sh 可以查看linux系统中装了几种shell

查看目前使用的是哪种shell
env查看当前操作系统的环境变量

chsh -s 修改使用的shells

#!/usr/bin/env python
#coding=utf-8

linux可以使用windows：
jdk的安装步骤：分区和iso文件都可以直接挂载到系统中
把iso文件挂载到linux系统中，操作系统需要用哪个分区，就把哪个分区挂载到系统中，不需要了就卸载分区
在虚拟机中设置，cd-ram中选择需要的jdk的iso文件
mount /mnt/cdrom/ 挂载文件到/mnt/cdrom中
umount /mnt/cdrom 卸载文件

安装好jdk后，其实用的还是之前redhat自带的jdk,ubuntu中没有自带的额jdk
需要修改环境配置文件：/etc/profile，配置完了一定要用export引用

如果只是想临时加入路径，可以直接输入命令export PATH=$PATH(是指已有的path):/root(新加的路径)，这只是临时生效

linux中已经定义好的变量：$SHELL,$PATH,$USER
echo 变量名 显示变量内容

linux中可以安装eclipse，但是启动必须先进入图形界面

安装mysql：/->home->mysql->bin->mysql
若想在任何目录下都可以直接运行mysql，需要修改配置文件
首先会从env命令中的PATH找能不能找到mysql命令
每个用户目录下都会有用户环境变量.profile文件，在里面添加路径即可，但是只限于当前用户可以直接在任何目录下都可以运行

在etc/profile是全局的环境变量，如果在这里修改，所有用户都可以在任何目录下运行

在/etc/.bashrc中可以指定某些程序在任意用户登陆的时候就启动，
如：添加/home/tomcat/bin/startup.sh start
在/home/用户名/.bashrc中指定程序，则只有指定用户登录时系统才会启动这些程序

winiso软件可以制作iso软件

windows分区：基本分区和扩展分区
基本分区、扩展分区之和不能超过4个
基本分区可以直接使用且不能再分区,即主分区一般就是操作系统所在的分区
扩展分区必须进行二次分区，即需要逻辑分区

/boot目录下其实是没有东西的，但是它是把放在硬盘上(都分过区)的引导信息相关文件的分区挂载到了/boot目录下，所以/boot目录下才会有数据
即所有东西其实都是挂载到根目录/下的，可以把分区挂载到自己指定的路径

df查看磁盘使用情况，看分区还有多少可以使用
df 目录全路径可以查看目录与哪块分区有联系的
fdisk -l查看分区情况得到/dev/sda1，/dev/sda1等等
(dev是设备，sda1是第几块分区，sd代表现在挂载的盘是SCSI硬盘，hd是IDE硬盘，a代表第几个硬盘，b代表第二个硬盘，1，2代表第一，二个分区；1-4代表主分区或扩展分区，5以后代表逻辑分区)
硬盘分为IDE硬盘和SCSI硬盘

netstat -anp可以监控网络状态信息，哪些端口在监听，也可以看到哪些ip地址连接了此操作系统，就可以把一些用户通过kill踢掉
oute查看路由表

任务调度命令crontab
设置任务：crontab -e就进入设置任务的页面，输入一些命令即可


进程是正在执行的程序
线程是(1)轻量级的进程
      (2)进程有独立的地址空间，线程没有，用户吧把进程启动时，操作系统就会从内存条给进程划分出一块内存使用，进程只能在这块区域内使用，分配的内存不一定是连续的。
进程如果读取了别的内存，则很有可能是病毒，也有可能会蓝屏
     (3)相对来说，线程不能独立存在，它是由进程创建的，thread创建    
     (4)相对讲，线程耗费的cpu和内存小于进程

alias给命令取别名：
ls -l /home
取别名为llh：
alias llh='ls -l /home'

runlevel命令可以查看当前的运行级别







